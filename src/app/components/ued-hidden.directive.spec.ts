import { UedHiddenDirective } from './ued-hidden.directive';

describe('UedHiddenDirective', () => {
  it('should create an instance', () => {
    const directive = new UedHiddenDirective();
    expect(directive).toBeTruthy();
  });
});
