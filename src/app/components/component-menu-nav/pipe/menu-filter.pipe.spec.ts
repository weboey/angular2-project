import { MenuFilterPipe } from './menu-filter.pipe.ts';

describe('MenuFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MenuFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
