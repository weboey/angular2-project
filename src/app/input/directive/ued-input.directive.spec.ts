import { UedInputDirective } from './ued-input.directive';

describe('UedInputDirective', () => {
  it('should create an instance', () => {
    const directive = new UedInputDirective();
    expect(directive).toBeTruthy();
  });
});
